services:
  screenstudio:
    build: .
    ports:
      - "3000:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - rtmp-relay

  rtmp-relay:
    build: ./server
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
